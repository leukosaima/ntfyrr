name: docker
on:
  workflow_dispatch: {}
  release:
    types:
      - published

permissions:
  contents: write
  packages: write
  id-token: write

jobs:
  docker:
    name: build-publish
    uses: leukosaima/gh-actions/.github/workflows/docker-github-builder.yaml@b0f5a3400d38b6bade95b12c04b69abd770a972f # main
    secrets: inherit
