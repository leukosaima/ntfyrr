name: docker
on:
  workflow_dispatch: {}
  release:
    types:
      - published

permissions:
  contents: write
  packages: write
  id-token: write

jobs:
  docker:
    name: build-publish
    uses: leukosaima/gh-actions/.github/workflows/docker-github-builder.yaml@3af67032ebad47047e0799050e12d0ddf912bac6 # main
    secrets: inherit
