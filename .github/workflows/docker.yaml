name: docker
on:
  workflow_dispatch: {}
  release:
    types:
      - published

permissions:
  contents: write
  packages: write
  id-token: write

jobs:
  docker:
    name: build-publish
    uses: leukosaima/gh-actions/.github/workflows/docker-github-builder.yaml@main
    secrets: inherit
