name: pr

on:
  pull_request:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  dotnet:
    uses: leukosaima/gh-actions/.github/workflows/dotnet-build.yaml@bd2ed14bf9edef8513872155b60038696e95e3d8 # main
    with:
      target: ntfyrr.sln
      dotnet-version: 10.0.x
      configuration: Release
