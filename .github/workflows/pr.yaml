name: pr

on:
  pull_request:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  dotnet:
    uses: leukosaima/gh-actions/.github/workflows/dotnet-build.yaml@56af9f257468e04fd140d2e61b846b8a54fd31f2 # main
    with:
      target: ntfyrr.sln
      dotnet-version: 10.0.x
      configuration: Release
